import { Inter,Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], preload: true});
export const lusitana = Lusitana({weight:"700",subsets: ['latin'], preload: true});